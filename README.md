# learning-Programing

C#

Kalkulator ✅ — 4 basic operations, division by zero handling
Lista zadań (To-Do) ✅ — add, remove, display tasks
Zgadnij liczbę — guess 1–100, higher/lower hints, attempt counter
Konwerter jednostek — temperature (°C/°F/K), length, weight
Konto bankowe — deposit, withdraw, transaction history
Menadżer kontaktów — search, add, save to .txt file


Python

Kalkulator ✅ — functions, try/except
Zgadnij liczbę — random, while loop, final score
Analizator tekstu — count words, chars, most frequent words
Quiz wiedzy — question dictionary, random order, score %
Lista zakupów — add/remove, save to CSV
Kalkulator BMI — input validation, weight categories, tips


Rust

Kalkulator ✅ — match, Result<>, error handling
Zmiana wielkości liter ✅ — to_uppercase/lowercase, iterators
Zgadnij liczbę ✅ — rand crate, loop, match
Silnia i Fibonacci — recursion vs iteration, u64, overflow handling
Mini lista zadań — struct Task, impl block, Vec, ownership & borrowing
Konwerter temperatury — unit enum, match, conversion functions, unit tests #[test]


C

Kalkulator — 4 basic operations, switch/case, division by zero
Zgadnij liczbę — rand(), while loop, higher/lower hints
Konwerter temperatury — functions, float arithmetic, user input
Silnia i Fibonacci — recursion vs iteration, unsigned long
Mini lista zadań — arrays, structs, pointers, basic CRUD
Manipulacja stringami — strlen, strcpy, strcat, reverse string, palindrome check

✅ = already done | Tasks ordered from easiest to hardest
